// Busca

let numeros = [10, 20, 30, 40];

// Verifica se o número 20 existe
console.log(numeros.includes(20)); // true

// Mostra a posição do número 30
console.log(numeros.indexOf(30)); // 2